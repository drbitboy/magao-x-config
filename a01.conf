# a01.conf -- magaoxMaths instance "a01" (host role: magaox01)
#
# otherDevName/otherValName keys per spec. Standard MagAOXApp config
# keys (logging, RT scheduling priority, etc.) are omitted -- their
# exact required set for magaoxMaths specifically is unconfirmed.

otherDevName=c02
otherValName=val
myVal=val
